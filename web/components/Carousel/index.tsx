import styled from 'styled-components';
import {color, space, typography, flexbox, layout, shadow} from 'styled-system';

const CarouselContainer = styled.div`
    
`;

export const OutcomesCarousel = (): JSX.Element => {
    return <></>;
};