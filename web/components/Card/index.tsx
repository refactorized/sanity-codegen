import styled from 'styled-components';

export interface StatCardProps {

};

export interface TestimonialCardProps {

};

export const StatCard = ({}: StatCardProps): JSX.Element => {
    return <></>;
};

export const TestimonialCard = ({}: StatCardProps): JSX.Element => {
    return <></>;
};